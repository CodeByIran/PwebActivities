// import sequelize from '../config/database.js'; // ajuste o caminho conforme seu projeto

// after(async () => {
//   console.log('Fechando conexão final do Sequelize...');
//   await sequelize.close();
// });

// package.json
// "test": "cross-env NODE_ENV=test mocha tests/**/*.test.js --file tests/global-teardown.test.js"

/* Para descomentar, selecione a parte e der um Ctrll + K 
        e dps Ctrll + U
        */

        /* Para Comentar , selecione a parte que deseja e der um Ctrll + K 
        e dps Ctrll + C
        */